# Read reviewed unit conversion rules

Rules are directed affine transforms using
`converted_value = original_value * scale + offset`.

## Usage

``` r
gx_unit_conversions()
```

## Value

A tibble of versioned conversion rules.
