# Return the Geoconnex publisher JSON-LD context

The context is a local, network-free mapping for publisher profile
1.0.0. A fresh copy is returned on every call.

## Usage

``` r
gx_context()
```

## Value

A named JSON-LD context list.
