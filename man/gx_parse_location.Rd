% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profiles.R
\name{gx_parse_location}
\alias{gx_parse_location}
\title{Parse Geoconnex monitoring-location profiles}
\usage{
gx_parse_location(x, strict = FALSE)
}
\arguments{
\item{x}{A \code{\link[=gx_jsonld]{gx_jsonld()}} object, parsed JSON-LD list, JSON string, or raw
JSON bytes.}

\item{strict}{Whether any warning/error diagnostic should abort after
deterministic parsing.}
}
\value{
A \code{gx_location} tibble with one row per supported location. Its exact
columns are \code{contract_version}, \code{site_uri}, \code{name}, \code{description},
\code{site_type}, \code{rdf_types}, \code{provider_uri}, \code{provider_name}, \code{provider_url},
\code{mainstem_uri}, \code{geometry_wkt}, \code{geometry_crs}, \code{landing_url}, \code{source_url},
and \code{diagnostics}. All are character except the two list-columns
\code{rdf_types} and \code{diagnostics}. Zero-row results preserve those types.
}
\description{
Extracts documented and known production Geoconnex location shapes from
safely expanded JSON-LD. WKT remains text at this protocol layer.
}
\section{Diagnostics and provenance}{

Each row's \code{diagnostics} entry has fixed character columns \code{severity}, \code{code},
\code{path}, and \code{message}, plus logical \code{recoverable}. The table's \code{diagnostics}
attribute includes document-level and row-level diagnostics, including for a
zero-row result. With \code{strict = TRUE}, any warning or error is reported as a
\code{gx_error_parser_strict} after deterministic parsing. The table retains exact
provenance URLs; its print method redacts query values and credentials.

This P0 contract is provisional: WKT remains text, duplicate relationships
remain diagnostic, and no canonical-site or typed-geometry contract is yet
asserted.
}
