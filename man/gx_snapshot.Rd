% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snapshot-public.R
\name{gx_snapshot}
\alias{gx_snapshot}
\title{Create a verified catalog-only snapshot}
\usage{
gx_snapshot(x, dir, fetch = FALSE, report = FALSE, overwrite = FALSE)
}
\arguments{
\item{x}{A validated \code{gx_catalog} object.}

\item{dir}{New destination directory beneath an existing safe parent.}

\item{fetch}{Must be \code{FALSE}; fetching during snapshot creation is not
supported.}

\item{report}{Must be \code{FALSE}; report rendering is not supported.}

\item{overwrite}{Must be \code{FALSE}; existing destinations are preserved.}
}
\value{
A validated \code{gx_snapshot} object containing the normalized absolute
path, final \code{\link[=gx_snapshot_verify]{gx_snapshot_verify()}} evidence, and an exact catalog-only
scope declaration.
}
\description{
Writes one validated \code{\link[=gx_catalog]{gx_catalog()}} to a new snapshot directory using the
fixed deterministic catalog CSV profile and \code{manifest.json}. Creation occurs
in a sibling staging directory, the closed tree is verified before atomic
exposure, and the final directory is verified again. Existing destinations
are never replaced, repaired, or removed.
}
\details{
This first public snapshot contract is deliberately catalog-only.
\code{fetch}, \code{report}, and \code{overwrite} must remain \code{FALSE}. It creates no
Frictionless data package, fetched or harmonized resources, report, loading
contract, signature, or replay authority. It performs no network, DNS,
discovery, cache, or optional-package work.
}
