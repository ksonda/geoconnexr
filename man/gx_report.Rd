% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package-report-public.R
\name{gx_report}
\alias{gx_report}
\title{Access a verified geoconnexr package report}
\usage{
gx_report(x, output = NULL)
}
\arguments{
\item{x}{A report-bearing \code{gx_package}, \code{gx_package_loaded}, or package
directory path.}

\item{output}{\code{NULL} for in-memory access, or one absent HTML file path.}
}
\value{
A validated \code{gx_report} object containing the verified package load,
report manifest descriptor, exact HTML bytes, optional output path, fixed
scope metadata, and report-access identity.
}
\description{
Loads the fixed HTML report from a report-bearing package as exact bounded
bytes, revalidates its isolated structure and manifest binding, and returns
read-only evidence. When \code{output} is supplied, the same bytes are atomically
written to one absent file beneath an existing safe parent.
}
\details{
Access is offline and never invokes Quarto. The report is redacted,
execution-disabled, cache-disabled, unsigned, and non-replayable.
}
