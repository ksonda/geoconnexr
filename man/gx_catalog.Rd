% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog-discovery.R
\name{gx_catalog}
\alias{gx_catalog}
\title{Discover a bounded Geoconnex catalog}
\usage{
gx_catalog(
  aoi,
  site_uri = NULL,
  profiles = NULL,
  max_sites = 25L,
  client = gx_client("pid"),
  graph_client = gx_client("graph"),
  reference_client = gx_client("reference")
)
}
\arguments{
\item{aoi}{A validated object returned by \code{\link[=gx_aoi]{gx_aoi()}}.}

\item{site_uri}{Optional unique canonical HTTP(S) site PID URIs. When
supplied, automatic AOI membership discovery is skipped.}

\item{profiles}{Optional named list of local JSON-LD inputs, with names
exactly matching \code{site_uri}. Each value may be a decoded JSON-LD list, a
JSON string, raw JSON bytes, or a \code{gx_jsonld} object. Supplying profiles
also requires \code{site_uri} and performs no PID profile requests.}

\item{max_sites}{Maximum graph rows or explicit site PIDs, from 1 through
100.}

\item{client}{A PID client from \code{\link[=gx_client]{gx_client()}}.}

\item{graph_client}{A graph client from \code{\link[=gx_client]{gx_client()}}.}

\item{reference_client}{A reference client from \code{\link[=gx_client]{gx_client()}}.}
}
\value{
A validated \code{gx_catalog} with typed sites, flattened datasets,
recoverable problems, request evidence, and procedural completeness.
}
\description{
Builds the public catalog value object used by \code{\link[=gx_fetch_plan]{gx_fetch_plan()}}. With no
explicit site PIDs, the function resolves identifier AOIs through the
Geoconnex reference service, executes one bounded \code{sites_in_aoi} graph page,
then retrieves each selected PID's JSON-LD profile. Supplying \code{site_uri}
skips graph discovery and retrieves exactly those profiles; this is useful
for reproducible examples, but does not assert that the PIDs fall inside the
AOI.
Alternatively, \code{profiles} can provide those exact profiles as local JSON-LD
inputs accepted by \code{\link[=gx_parse_location]{gx_parse_location()}}. This performs no PID profile
request and is intended for offline catalog construction or for providers
whose distribution description is not yet published through a PID.
}
\details{
Discovery is deliberately single-page and sequential. Reaching \code{max_sites}
records a partial, truncated catalog rather than following a graph page.
Individual profile failures remain visible in \code{catalog$problems} and
completeness metadata while unrelated sites continue.
}
