% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lookup-assets.R
\name{gx_mainstem_lookup_install}
\alias{gx_mainstem_lookup_install}
\title{Explicitly install the pinned mainstem lookup}
\usage{
gx_mainstem_lookup_install(
  source = c("release", "file"),
  file = NULL,
  version = "v3.2",
  force = FALSE,
  confirm = interactive(),
  offline = getOption("geoconnexr.offline", FALSE),
  data_dir = gx_default_data_dir()
)
}
\arguments{
\item{source}{Either \code{"release"} for the pinned upstream release asset or
\code{"file"} for a local import.}

\item{file}{Local CSV path required by \code{source = "file"}; otherwise \code{NULL}.}

\item{version}{Pinned lookup release. Currently only \code{"v3.2"} is
registered.}

\item{force}{Replace an existing installation after the replacement has
fully verified.}

\item{confirm}{Prompt before writing or downloading when \code{TRUE}.
Non-interactive callers must pass \code{FALSE} explicitly if they set this
argument to \code{TRUE} through shared configuration.}

\item{offline}{Whether network access is prohibited. Local imports remain
available offline.}

\item{data_dir}{Persistent package data directory, separate from the HTTP
cache.}
}
\value{
The verified one-row result from \code{\link[=gx_mainstem_lookup_info]{gx_mainstem_lookup_info()}}.
}
\description{
Installs the optional \code{ref_rivers} NHDPlusV2 COMID lookup into a separate,
package-owned data directory. This is the only geoconnexr operation that
downloads the disclosed 120,422,425-byte v3.2 asset; crosswalk calls never
install or refresh it implicitly. \code{source = "file"} imports the same pinned
bytes from a local file and is suitable for air-gapped use.
}
\details{
The transfer is streamed to disk with redirects disabled at the transport
layer and validated hop by hop. The exact byte count, SHA-256 digest, CSV
schema, row count, known answers, and provenance receipt must all verify
before an atomic replacement. Included mainstems were non-superseded when
v3.2 was generated; installation does not check current service state.
}
