% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jsonld.R
\name{gx_jsonld}
\alias{gx_jsonld}
\title{Retrieve bounded Geoconnex JSON-LD}
\usage{
gx_jsonld(uri, as = c("expanded", "raw", "text"), client = gx_client("pid"))
}
\arguments{
\item{uri}{One HTTP(S) identifier.}

\item{as}{Representation exposed in \code{document}: standards-expanded, parsed
source JSON, or source JSON text. The object always retains both parsed
source and expanded forms for the profile parsers.}

\item{client}{A PID client created by \code{\link[=gx_client]{gx_client()}}.}
}
\value{
A \code{gx_jsonld} list with these fields:
\itemize{
\item \code{contract_version} and \code{representation} identify the experimental output
contract and the form selected in \code{document}.
\item \code{pid_uri}, \code{landing_url}, and \code{source_url} preserve identifier, redirect,
and representation provenance. \code{source_url} is exact and can contain query
credentials; do not write it to logs without redaction.
\item \code{media_type}, \code{retrieval_mode}, \code{retrieved_at}, \code{content_sha256},
\code{content_bytes}, and \code{response_sha256} describe the selected source.
\item \code{document}, \code{source_document}, and \code{expanded} contain the selected form,
parsed source JSON-LD, and standards-expanded JSON-LD, respectively.
\item \code{resolution} is the \code{\link[=gx_resolve]{gx_resolve()}} row, \code{requests} is a physical-request
ledger with redacted URLs, and \code{diagnostics} has fixed columns \code{severity},
\code{code}, \code{path}, \code{message}, and \code{recoverable}.
}
}
\description{
Resolves one identifier, negotiates JSON-LD, and falls back to embedded
JSON-LD or one advertised JSON-LD alternate on an HTML landing page. Every
network request uses the bounded package transport. Remote contexts are
replaced only from the bundled allowlist before standards-based expansion;
arbitrary context loading is disabled.
}
\section{Processing boundary}{

Parsers receive already-bounded bytes and cannot make network requests.
HTML is parsed with \code{NONET}; only inline scripts and one safe advertised
alternate are considered. M2 disables transport retries so every physical
request fits the same ledger and cumulative budget. The \code{as} argument changes
only \code{document}, not validation or expansion.
}

\section{Safety limits}{

Options provide fail-closed ceilings. Defaults are 16 requests; twice the
client's per-response limit in cumulative response bytes; 2 MiB of local
JSON; depth 64; 10,000 serialized members; 512 KiB of contexts; 16 MiB of
expanded JSON; and 64 HTML candidates. Profile parsing additionally limits
one identity to 64 defining fragments and dataset output to 10,000 rows.
Invalid limit values fail rather than disabling a ceiling.

All retrieval, parsing, context, and budget failures inherit from
\code{gx_error_jsonld}. See \code{\link[=gx_parse_location]{gx_parse_location()}} and \code{\link[=gx_parse_datasets]{gx_parse_datasets()}} for
tolerant profile-level diagnostics.
}
