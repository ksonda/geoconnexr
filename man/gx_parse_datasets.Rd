% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profiles.R
\name{gx_parse_datasets}
\alias{gx_parse_datasets}
\title{Parse datasets described by Geoconnex location profiles}
\usage{
gx_parse_datasets(x, strict = FALSE)
}
\arguments{
\item{x}{A \code{\link[=gx_jsonld]{gx_jsonld()}} object, parsed JSON-LD list, JSON string, or raw
JSON bytes.}

\item{strict}{Whether any warning/error diagnostic should abort after
deterministic parsing.}
}
\value{
A \code{gx_datasets} tibble with stable identifiers, classifier facts,
provenance, and structured diagnostics. Its exact columns are
\code{contract_version}, \code{site_uri}, \code{dataset_id}, \code{distribution_id},
\code{variable_id}, \code{dataset_uri}, \code{dataset_name}, \code{dataset_description},
\code{temporal_coverage}, \code{temporal_start}, \code{temporal_end}, \code{variable_uri},
\code{variable_name}, \code{unit_uri}, \code{unit_label}, \code{measurement_technique},
\code{distribution_url}, \code{media_type}, \code{conforms_to}, \code{provider_uri},
\code{provider_name}, \code{provider_url}, \code{license}, \code{access_rights}, \code{handler_id},
\code{fetchable}, \code{source_url}, and \code{diagnostics}. Temporal columns are UTC
\code{POSIXct}, \code{fetchable} is logical, \code{conforms_to} and \code{diagnostics} are list
columns, and all others are character. Zero-row results preserve types.

Dataset identifiers follow the installed language-neutral identity contract.
Absolute HTTP(S) and opaque IRIs preserve semantic fragments; label fallback
uses UTF-8 NFC and Unicode default case folding. Unsafe distribution URLs are
retained as provenance but never marked fetchable. \code{gx_error_parser_budget}
is raised before a distribution-by-variable product exceeds the configured
row ceiling. Diagnostics and strict-mode behavior match \code{\link[=gx_parse_location]{gx_parse_location()}}.
}
\description{
Produces one row per dataset by distribution by variable. A missing
distribution or variable contributes one explicit \code{NA} dimension so an
incomplete real-world dataset is not silently discarded.
}
