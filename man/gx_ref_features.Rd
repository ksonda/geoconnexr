% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference.R
\name{gx_ref_features}
\alias{gx_ref_features}
\title{Retrieve bounded reference features}
\usage{
gx_ref_features(
  collection,
  query = list(),
  bbox = NULL,
  limit = 1000L,
  allow_unbounded = FALSE,
  client = gx_client("reference")
)
}
\arguments{
\item{collection}{One advertised collection identifier.}

\item{query}{Named list of simple equality filters. Every name and scalar
value is checked against \code{\link[=gx_ref_queryables]{gx_ref_queryables()}}.}

\item{bbox}{Optional numeric bounding box with four or six coordinates.}

\item{limit}{Positive overall row limit, not merely a page size.}

\item{allow_unbounded}{Explicit opt-in for a request without \code{query} or
\code{bbox}; row, page, response-byte, and cumulative-byte ceilings still apply.}

\item{client}{A reference client created by \code{\link[=gx_client]{gx_client()}}.}
}
\value{
A \code{gx_ref_features} simple-feature table. Top-level GeoJSON IDs are
normalized to character \code{feature_id}, and hydrologic identifier property
columns are also character. The \code{gx_reference} attribute records
\code{truncated}, \code{stop_reason}, \code{number_matched}, page and byte counts,
diagnostics, and the redacted request ledger.
}
\description{
Validates simple property filters against the collection's advertised
queryables and follows same-endpoint \code{rel=next} links until the requested
row limit or a deterministic stop condition is reached.
Query-bearing responses follow the package privacy policy and are not
persisted, so offline filtered replay is not promised.
}
\section{Budgets}{

The per-response ceiling comes from \code{client$max_bytes}. Additional options
bound the workflow: \code{geoconnexr.ref_page_size} (default 1000),
\code{geoconnexr.ref_max_pages} (100), \code{geoconnexr.ref_total_bytes} (eight times
the client response ceiling), and \code{geoconnexr.ref_max_members} (250,000 JSON
members per response). Filter values default to 4,096 bytes through
\code{geoconnexr.ref_max_query_value_bytes}, and every reference URL defaults to
16,384 bytes through \code{geoconnexr.ref_max_url_bytes}. Invalid option values
fail before an items request.
}
