% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package-hydrate.R
\name{gx_package_hydrate}
\alias{gx_package_hydrate}
\title{Hydrate verified package-owned tables with fixed storage types}
\usage{
gx_package_hydrate(dir)
}
\arguments{
\item{dir}{Existing package directory created with the fixed public
\code{\link[=gx_package]{gx_package()}} profile.}
}
\value{
A validated \code{gx_package_hydrated} object containing typed
package-owned tables, character-only provider-native tables, the complete
canonical table evidence, fixed scope metadata, and deterministic
hydration identity.
}
\description{
Loads a package through \code{\link[=gx_package_tables]{gx_package_tables()}} and applies only the exact
storage types declared by the fixed package profile. Catalog tables receive
the same redacted geometry, UTC timestamp, logical, and JSON-array typing as
\code{\link[=gx_snapshot_catalog_view]{gx_snapshot_catalog_view()}}. Package-owned fetch indexes and harmonization
tables receive explicit integer, double, logical, and UTC timestamp types.
Parquet observations are read in memory through the reviewed Arrow
capability and must reproduce the exact fixed typed schema.
}
\details{
Provider-native CSV tables remain character-only and raw provider resources
remain opaque in the embedded loading evidence. The result is a redacted,
read-only inspection view; it does not reconstruct a live \code{gx_catalog},
\code{gx_fetched}, or \code{gx_harmonized} object, authenticate the unsigned manifest,
write, refresh, or replay the package.
}
