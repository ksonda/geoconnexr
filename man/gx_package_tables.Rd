% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package-tables.R
\name{gx_package_tables}
\alias{gx_package_tables}
\title{Parse verified package CSV resources as canonical character tables}
\usage{
gx_package_tables(dir)
}
\arguments{
\item{dir}{Existing package directory created with the fixed public
\code{\link[=gx_package]{gx_package()}} profile.}
}
\value{
A validated \code{gx_package_tables} object containing the embedded
byte-preserving load evidence, path-named canonical character tables,
fixed scope metadata, and deterministic table-view identity.
}
\description{
Loads a fixed-profile package with \code{\link[=gx_package_load]{gx_package_load()}} and parses every CSV
resource from its already verified in-memory bytes. Parsing uses the same
strict bytewise CSV implementation as snapshot loading and admits only
quote-all canonical UTF-8/LF tables under the package resource, column, and
field ceilings.
}
\details{
All CSV columns remain character vectors. Parquet and native raw resources
remain available only through the embedded \code{gx_package_loaded} object. This
function does not reconstruct a live catalog, fetched result, or harmonized
result, infer types or semantics, authenticate the manifest, write, refresh,
or replay.
}
