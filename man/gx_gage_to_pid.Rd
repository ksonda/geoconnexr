% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosswalks.R
\name{gx_gage_to_pid}
\alias{gx_gage_to_pid}
\title{Map provider gage identifiers to Geoconnex reference PIDs}
\usage{
gx_gage_to_pid(provider_id, client = gx_client("reference"))
}
\arguments{
\item{provider_id}{A character vector of opaque provider identifiers.
Numeric values, missing values, blanks, invalid UTF-8, and control
characters are rejected without network access.}

\item{client}{A reference client created by \code{\link[=gx_client]{gx_client()}}.}
}
\value{
A \code{gx_gage_crosswalk} tibble with one row for a unique match, one
explicit sentinel row for no match, or every distinct match when a
provider identifier is ambiguous. Identifier columns are character. The
\code{gx_crosswalk} attribute contains aggregate counts, diagnostics, retrieval
time, completeness, and the redacted reference request ledger.
}
\description{
Queries the native reference client using the advertised \code{provider_id}
property and verifies that every returned feature has consistent provider,
feature, property, and PID identities. Inputs are deduplicated for transport
and expanded back into their original order.
}
\details{
This experimental M4 slice retains advertised \code{mainstem_uri} values without
asserting that they belong to a selected current mainstem vintage. Query-
bearing reference responses are intentionally non-cacheable, so offline
crosswalk retrieval is not promised.
}
\section{Budgets}{

\code{geoconnexr.crosswalk_max_inputs} defaults to 100,
\code{geoconnexr.crosswalk_max_matches} to 100,
\code{geoconnexr.crosswalk_max_rows} to 1,000,
\code{geoconnexr.crosswalk_max_requests} to 256, and
\code{geoconnexr.crosswalk_total_bytes} to eight times the client response
ceiling. Invalid limits or incomplete reference pagination fail closed.
}
