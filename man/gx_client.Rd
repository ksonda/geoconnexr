% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{gx_client}
\alias{gx_client}
\title{Create a bounded Geoconnex protocol client}
\usage{
gx_client(
  endpoint = c("graph", "reference", "pid"),
  timeout = 30,
  retries = 3L,
  min_interval = getOption("geoconnexr.min_interval", 0.1),
  max_bytes = 2 * 1024^2,
  cache = TRUE,
  offline = getOption("geoconnexr.offline", FALSE),
  cache_dir = gx_default_cache_dir()
)
}
\arguments{
\item{endpoint}{One of \code{"graph"}, \code{"reference"}, or \code{"pid"}.}

\item{timeout}{Per-attempt timeout in seconds.}

\item{retries}{Number of physical retry attempts after the initial request,
from zero through 1,000.}

\item{min_interval}{Minimum seconds between physical attempts reserved for
the same hostname. Defaults to 0.1 seconds.}

\item{max_bytes}{Maximum identity-encoded response bytes per physical
attempt.}

\item{cache}{Whether successful and redirect responses may use the package
cache.}

\item{offline}{Whether requests must be satisfied from valid cache entries.}

\item{cache_dir}{Cache directory.}
}
\value{
An object of class \code{gx_client}.
}
\description{
A client records endpoint-specific request policy. Network calls remain
lazy: constructing a client never contacts a service or writes to its cache.
}
\section{Retries}{

The package retries HTTP 429, 500, 502, 503, and 504 responses and transport
failures. Every physical attempt revalidates DNS and is recorded for
downstream request/byte budgets. \code{Retry-After} is honored up to
\code{geoconnexr.retry_max_delay}, which defaults to 60 seconds; a larger server
minimum stops retrying instead of being shortened.
}

\section{Host throttling}{

Physical attempts across clients in one R process share a hostname schedule.
Retries and manual redirect hops reserve new slots, while cache hits and
offline misses do not. \code{min_interval = 0} adds no interval of its own but
still honors a preceding positive reservation from another client. Bounded
concurrent dispatch remains future work.
}
