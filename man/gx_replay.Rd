% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replay-public.R
\name{gx_replay}
\alias{gx_replay}
\title{Inspect a verified stored snapshot or package offline}
\usage{
gx_replay(manifest, dir = NULL, refresh = FALSE, ...)
}
\arguments{
\item{manifest}{A \code{gx_snapshot}, \code{gx_package}, \code{gx_package_loaded}, existing
snapshot/package directory, or exact \code{manifest.json} file path.}

\item{dir}{Must be \code{NULL}. Replay publication to a destination is deferred.}

\item{refresh}{Must be \code{FALSE}. Recipe execution and refresh are deferred.}

\item{...}{Must be empty; future replay options are not yet authorized.}
}
\value{
A validated \code{gx_replay} object containing the fixed source kind and
stage, final verification evidence, typed read-only view, optional report
evidence, explicit limitations, and deterministic inspection identity.
}
\description{
Verifies and loads one existing fixed geoconnexr catalog snapshot or data
package without executing its recipe. Catalog snapshots return the exact
typed redacted \code{\link[=gx_snapshot_catalog_view]{gx_snapshot_catalog_view()}}; packages return the exact typed
\code{\link[=gx_package_hydrate]{gx_package_hydrate()}} view and, when present, read-only \code{\link[=gx_report]{gx_report()}}
evidence. Verification is repeated around loading by the underlying fixed
loaders, and the result is rebound to the current closed tree.
}
\details{
This checkpoint supports only \code{refresh = FALSE}. It performs no network,
DNS, cache, optional runtime, write, repair, destination creation,
external Frictionless execution, authenticity check, or live workflow-object
reconstruction. It is stored-state inspection, not procedural replay.
}
