% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch-public.R
\name{gx_fetch_plan}
\alias{gx_fetch_plan}
\title{Build a bounded fetch plan}
\usage{
gx_fetch_plan(
  catalog,
  time = NULL,
  handlers = gx_handlers(),
  max_datasets = 100L,
  max_bytes = 1e+09
)
}
\arguments{
\item{catalog}{A validated \code{gx_catalog} object.}

\item{time}{\code{NULL}, two UTC \code{POSIXct} values, or an exact list containing
\code{start} and \code{end} UTC bounds.}

\item{handlers}{The exact built-in registry returned by \code{\link[=gx_handlers]{gx_handlers()}}.}

\item{max_datasets}{Maximum number of distributions to select.}

\item{max_bytes}{Maximum aggregate encoded bytes and maximum aggregate
decoded bytes.}
}
\value{
A validated \code{gx_fetch_plan} object.
}
\description{
Collapses a validated \code{gx_catalog} to one deterministic row per
distribution, intersects requested time coverage, applies the built-in
first-match handler registry, and records count and byte budgets. Planning
performs no DNS lookup or provider request.
}
\details{
The frozen M7 contract accepts only the built-in registry returned by
\code{\link[=gx_handlers]{gx_handlers()}}. Runtime registration is deferred. \code{max_bytes} is applied
independently to encoded and decoded response budgets, and each selected
distribution receives at most one physical-attempt reservation.
}
