% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference.R
\name{gx_ref_feature}
\alias{gx_ref_feature}
\title{Retrieve one reference feature with compatible fallbacks}
\usage{
gx_ref_feature(collection, id, client = gx_client("reference"))
}
\arguments{
\item{collection}{One advertised collection identifier.}

\item{id}{One feature identifier. It is always returned as character.}

\item{client}{A reference client created by \code{\link[=gx_client]{gx_client()}}.}
}
\value{
A one-row \code{gx_ref_feature} simple-feature table. Its
\code{gx_reference} attribute records \code{retrieval_mode}, \code{complete}, attempts,
diagnostics, and a redacted ledger for every physical response or transport
attempt in the workflow, plus cache retrievals. A stage that fails before
receiving a response remains visible in \code{attempts} with a missing status.
}
\description{
Tries the OGC item route first, then a collection filter using the
queryable marked \code{x-ogc-role: id}, and finally direct JSON-LD negotiation on
the item URL. Every successful path verifies the requested identity. The
JSON-LD path is marked incomplete because it can expose fewer properties
than GeoJSON.
}
