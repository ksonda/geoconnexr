# NLDI fixtures

`huc12pp-010100020101.min.geojson` preserves the exact response JSON observed
from the USGS NLDI `huc12pp` item endpoint on 2026-08-15, with one final LF
added for the repository text profile. Its source URL, live and stored digests,
and checked companion cases are recorded in
`data-raw/spike/huc12pp-contract-evidence-v1.json`.
